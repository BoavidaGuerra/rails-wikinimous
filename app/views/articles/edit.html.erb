<%= render 'navbar' %>
<div class="container">
  <div class="row">
    <div class="col-sm-8 col-sm-offset-2">
      <h1>Editing article</h1>
      <%= render 'form', article: @article %>
      <%= link_to 'Show', article_path(@article.id)  %> | <%= link_to 'Back', articles_path %>
    </div>
  </div>
</div>
